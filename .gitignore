# Node
node_modules/
.pnpm-store/
*.log
npm-debug.log*
pnpm-debug.log*

# TypeScript build output
dist/
build/
lib/
*.tsbuildinfo

# Test coverage
coverage/

# E2E（Maestro）跑 takeScreenshot 產生的截圖（相對 CWD 落地；本地驗證用、非資產）
e2e-*.png

# Design prototype 離線單檔（~7MB/檔，React/Babel/字體 inline 的產物，可由 prototype/ 重新產生）
*.standalone.html

# Secrets (Firebase plist, OAuth keys, etc.)
.secrets/
GoogleService-Info.plist
google-services.json
*.env.sprint1-reference

# Env
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
!.vscode/extensions.json
!.vscode/settings.json.example

# Expo / RN (Sprint 1+)
.expo/
.expo-shared/
# Native projects are generated by `expo prebuild` (CNG) — regenerated, not committed
apps/mobile/ios/
apps/mobile/android/
# Native build artifacts (any location)
**/ios/Pods/
**/android/.gradle/
**/android/app/build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
# Local emulator persistence (import/export snapshot — never committed)
.emulator-data/

# Claude（僅精準放行 feynman-dojo skill；其餘含 openspec-* 本地 skill 照舊忽略）
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/feynman-dojo/
